# RIVEL Architecture: Secure Backend Proxy
# 
# The Android client application communicates directly with the RIVEL Backend Proxy.
# The GEMINI_API_KEY is stored and processed exclusively server-side (see /server directory)
# and is NOT embedded into the Android APK.
#
# Server-side configuration:
# GEMINI_API_KEY=YOUR_SERVER_SIDE_KEY
